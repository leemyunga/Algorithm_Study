class Solution {
    public int solution(String message) {
        int letterSize = 2;  // 글자 크기: 2cm

        // 편지지의 최소 가로길이를 계산
        int totalLength = message.length() * letterSize;

        return totalLength;
    }
}

